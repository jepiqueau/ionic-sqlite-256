import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-sqlite-256',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
